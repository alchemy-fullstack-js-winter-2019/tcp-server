const chatroom = require('../lib/chatroom');

describe('ChatRoom', () => {
  let chatroom = null;

});

it('can add a client', done => {

});

it('asserts that a name was returned as the username property of the client', done=> {

});

it('asserts that the name was assigned as a property to the client object' done => {

});

it('passes in the username returned from the add call and asserts the object is the same as what you supplied to the add call', done => {

})