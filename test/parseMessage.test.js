describe('parseMessage', () => {
  it('parse the components out of a string', ()
  expect(parseMessage('@cmd:param some text'))
    command: 'cmd',
    arg: 'para',
    text:'some text'
    });
  });
});